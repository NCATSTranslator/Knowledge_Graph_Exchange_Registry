# Putative KGX Docker Process...
# not sure how this will work in practice?
FROM python:3.8

RUN python -m pip install --upgrade pip && pip install wheel && \
    pip3 install --no-cache-dir kgx

EXPOSE 8100

ENTRYPOINT ["python"]

CMD ["-m", "kgea.server.kgx_validator"]
