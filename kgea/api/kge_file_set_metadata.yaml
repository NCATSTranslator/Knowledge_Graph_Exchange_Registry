id: "${kg_id}"
version:  "${kg_version}"
# TODO: we can perhaps user comments about File Set revisions here
revisions: >-
  ${revisions}
submitter:
  name: "${submitter}"
  email: "${submitter_email}"
# SmartAPI Registered Server access
# point for data (with KGE Archive Web Service)
size: "${size}"
access: "${host}/${kg_id}/${kg_version}"
