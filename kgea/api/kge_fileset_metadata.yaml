id: "${kg_id}"
biolink_model_release:  "${biolink_model_release}"
fileset_version:  "${fileset_version}"
# TODO: we can perhaps user comments about File Set revisions here
revisions: ${revisions}
submitter:
  name: "${submitter_name}"
  email: "${submitter_email}"
# SmartAPI Registered Server access
# point for data (with KGE Archive Web Service)
access: "${host}/${kg_id}/${fileset_version}"
size: ${size}
files:
${files}
